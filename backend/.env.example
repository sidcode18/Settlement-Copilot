# Settlement Copilot Environment Variables
# Set one API key to enable real LLM reasoning. Leave both blank to use fallback mode.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional database path override. Defaults to data/settlement.db.
# DB_PATH=data/settlement.db
